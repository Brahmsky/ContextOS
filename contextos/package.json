{
  "name": "contextos",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "run:cli": "node --enable-source-maps dist/apps/cli/src/run.js",
    "run:api": "node --enable-source-maps dist/apps/api/src/server.js"
  }
}
